// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// TicketComment is a reply or internal note on a ticket.
export type TicketComment = {
  id: string | undefined;
  tenantId: number | undefined;
  ticketId: string | undefined;
  body: string | undefined;
  // internal notes are visible only to agents, not the requester.
  internal: boolean | undefined;
  // author_id is the internal user who wrote the comment (0 if external).
  authorId: number | undefined;
  authorName: string | undefined;
  // author_email is set when the comment came from an external requester reply.
  authorEmail: string | undefined;
  createTime: wellKnownTimestamp | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type CreateCommentRequest = {
  //
  // Behaviors: REQUIRED
  ticketId: string | undefined;
  //
  // Behaviors: REQUIRED
  body: string | undefined;
  internal: boolean | undefined;
};

export type CreateCommentResponse = {
  comment: TicketComment | undefined;
};

export type ListCommentsRequest = {
  //
  // Behaviors: REQUIRED
  ticketId: string | undefined;
};

export type ListCommentsResponse = {
  comments: TicketComment[] | undefined;
  total: number | undefined;
};

export type DeleteCommentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export interface TicketCommentService {
  CreateComment(request: CreateCommentRequest): Promise<CreateCommentResponse>;
  ListComments(request: ListCommentsRequest): Promise<ListCommentsResponse>;
  DeleteComment(request: DeleteCommentRequest): Promise<wellKnownEmpty>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createTicketCommentServiceClient(
  handler: RequestHandler
): TicketCommentService {
  return {
    CreateComment(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.ticketId) {
        throw new Error("missing required field request.ticket_id");
      }
      const path = `v1/tickets/${request.ticketId}/comments`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketCommentService",
        method: "CreateComment",
      }) as Promise<CreateCommentResponse>;
    },
    ListComments(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.ticketId) {
        throw new Error("missing required field request.ticket_id");
      }
      const path = `v1/tickets/${request.ticketId}/comments`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketCommentService",
        method: "ListComments",
      }) as Promise<ListCommentsResponse>;
    },
    DeleteComment(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/comments/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "TicketCommentService",
        method: "DeleteComment",
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// RuleCondition is one row of the Cloudflare-style rule builder.
export type RuleCondition = {
  // field: subject | body | from | fromName | recipient | hasAttachments
  field: string | undefined;
  // operator: contains | not_contains | equals | not_equals | starts_with | ends_with | matches
  // (ignored for the boolean field hasAttachments; value is "true"/"false")
  operator: string | undefined;
  value: string | undefined;
};

// RuleAction is one row of the "Then apply" block. A rule may have several.
export type RuleAction = {
  // type: tag | assign | status | priority
  type: string | undefined;
  // tag: kind (TAG|CATEGORY) and the names to create/apply.
  tagKind: string | undefined;
  tagNames: string[] | undefined;
  // assign: internal user id to assign the ticket to (0 = unassign).
  assigneeId: number | undefined;
  // status: TICKET_STATUS_* enum-name string.
  status: string | undefined;
  // priority: TICKET_PRIORITY_* enum-name string.
  priority: string | undefined;
};

// TicketRule auto-applies tags/categories to inbound tickets.
export type TicketRule = {
  id: string | undefined;
  tenantId: number | undefined;
  name: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  // match: ALL (AND) or ANY (OR)
  match: string | undefined;
  conditions: RuleCondition[] | undefined;
  // expression overrides conditions when set (raw CEL).
  expression: string | undefined;
  // tag_kind/tag_names are the legacy single tag action, kept for
  // backward compatibility; new rules use the actions list.
  tagKind: string | undefined;
  tagNames: string[] | undefined;
  createTime: wellKnownTimestamp | undefined;
  updateTime: wellKnownTimestamp | undefined;
  actions: RuleAction[] | undefined;
};

export type ListRulesRequest = {
};

export type ListRulesResponse = {
  rules: TicketRule[] | undefined;
};

export type GetRuleRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetRuleResponse = {
  rule: TicketRule | undefined;
};

export type RuleInput = {
  name: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  match: string | undefined;
  conditions: RuleCondition[] | undefined;
  expression: string | undefined;
  tagKind: string | undefined;
  tagNames: string[] | undefined;
  actions: RuleAction[] | undefined;
};

export type CreateRuleRequest = {
  rule: RuleInput | undefined;
};

export type CreateRuleResponse = {
  rule: TicketRule | undefined;
};

export type UpdateRuleRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  rule: RuleInput | undefined;
};

export type UpdateRuleResponse = {
  rule: TicketRule | undefined;
};

export type DeleteRuleRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export interface TicketRuleService {
  ListRules(request: ListRulesRequest): Promise<ListRulesResponse>;
  GetRule(request: GetRuleRequest): Promise<GetRuleResponse>;
  CreateRule(request: CreateRuleRequest): Promise<CreateRuleResponse>;
  UpdateRule(request: UpdateRuleRequest): Promise<UpdateRuleResponse>;
  DeleteRule(request: DeleteRuleRequest): Promise<wellKnownEmpty>;
}

export function createTicketRuleServiceClient(
  handler: RequestHandler
): TicketRuleService {
  return {
    ListRules(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/rules`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketRuleService",
        method: "ListRules",
      }) as Promise<ListRulesResponse>;
    },
    GetRule(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/rules/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketRuleService",
        method: "GetRule",
      }) as Promise<GetRuleResponse>;
    },
    CreateRule(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/rules`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketRuleService",
        method: "CreateRule",
      }) as Promise<CreateRuleResponse>;
    },
    UpdateRule(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/rules/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "TicketRuleService",
        method: "UpdateRule",
      }) as Promise<UpdateRuleResponse>;
    },
    DeleteRule(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/rules/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "TicketRuleService",
        method: "DeleteRule",
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// Lifecycle states a ticket moves through.
export type TicketStatus =
  | "TICKET_STATUS_UNSPECIFIED"
  | "TICKET_STATUS_OPEN"
  | "TICKET_STATUS_IN_PROGRESS"
  | "TICKET_STATUS_PENDING"
  | "TICKET_STATUS_RESOLVED"
  | "TICKET_STATUS_CLOSED";
export type TicketPriority =
  | "TICKET_PRIORITY_UNSPECIFIED"
  | "TICKET_PRIORITY_LOW"
  | "TICKET_PRIORITY_NORMAL"
  | "TICKET_PRIORITY_HIGH"
  | "TICKET_PRIORITY_URGENT";
// Ticket is a support request, typically created from an inbound email
// forwarded by iris/KumoMTA, or created manually by an agent.
export type Ticket = {
  id: string | undefined;
  tenantId: number | undefined;
  // external_id dedupes inbound emails (the RFC822 Message-Id or the
  // X-Iris-Message-Id header). Unique per tenant when present.
  externalId: string | undefined;
  subject: string | undefined;
  description: string | undefined;
  // body_html is the raw HTML body of the email when present (rendered in a
  // sandboxed iframe by the UI). description holds the plain-text version.
  bodyHtml: string | undefined;
  status: TicketStatus | undefined;
  priority: TicketPriority | undefined;
  // source identifies where the ticket came from ("iris", "manual").
  source: string | undefined;
  requesterEmail: string | undefined;
  requesterName: string | undefined;
  // recipient is the support address the mail was delivered to
  // (X-Iris-Recipient), useful for routing/queues.
  recipient: string | undefined;
  // assignee_id is the internal user this ticket is assigned to (0 = unassigned).
  assigneeId: number | undefined;
  // assignee_name is denormalized for display (resolved from admin-service).
  assigneeName: string | undefined;
  commentCount: number | undefined;
  createBy: number | undefined;
  createTime: wellKnownTimestamp | undefined;
  updateTime: wellKnownTimestamp | undefined;
  // attachments are populated on GetTicket only (not in list responses).
  attachments: TicketAttachment[] | undefined;
  // tags/categories attached to the ticket (manual or rule-applied).
  tags: TicketTag[] | undefined;
};

// TicketAttachment is a file extracted from the inbound email, stored in
// S3/RustFS. Download via download_url (served by the module HTTP server
// through the admin gateway).
export type TicketAttachment = {
  id: string | undefined;
  ticketId: string | undefined;
  filename: string | undefined;
  contentType: string | undefined;
  size: number | undefined;
  contentId: string | undefined;
  inline: boolean | undefined;
  downloadUrl: string | undefined;
  createTime: wellKnownTimestamp | undefined;
};

// TicketTag is a label (kind=TAG) or category (kind=CATEGORY) attachable to
// tickets, manually or via rules. Defined here (not in tag.proto) because the
// Ticket message references it — keeps tag.proto's import one-directional.
export type TicketTag = {
  id: string | undefined;
  tenantId: number | undefined;
  name: string | undefined;
  kind: string | undefined;
  color: string | undefined;
  description: string | undefined;
  createTime: wellKnownTimestamp | undefined;
};

export type CreateTicketRequest = {
  //
  // Behaviors: REQUIRED
  subject: string | undefined;
  description: string | undefined;
  priority: TicketPriority | undefined;
  requesterEmail: string | undefined;
  requesterName: string | undefined;
  assigneeId: number | undefined;
};

export type CreateTicketResponse = {
  ticket: Ticket | undefined;
};

export type GetTicketRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetTicketResponse = {
  ticket: Ticket | undefined;
};

export type ListTicketsRequest = {
  page?: number;
  pageSize?: number;
  status?: TicketStatus;
  priority?: TicketPriority;
  // assignee_id filter: omit for all; -1 (4294967295) reserved for "unassigned".
  assigneeId?: number;
  query?: string;
};

export type ListTicketsResponse = {
  tickets: Ticket[] | undefined;
  total: number | undefined;
};

export type UpdateTicketRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  subject?: string;
  description?: string;
  priority?: TicketPriority;
};

export type UpdateTicketResponse = {
  ticket: Ticket | undefined;
};

export type DeleteTicketRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type AssignTicketRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  assigneeId: number | undefined;
};

export type AssignTicketResponse = {
  ticket: Ticket | undefined;
};

export type UpdateTicketStatusRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  status: TicketStatus | undefined;
};

export type UpdateTicketStatusResponse = {
  ticket: Ticket | undefined;
};

export interface TicketService {
  CreateTicket(request: CreateTicketRequest): Promise<CreateTicketResponse>;
  GetTicket(request: GetTicketRequest): Promise<GetTicketResponse>;
  ListTickets(request: ListTicketsRequest): Promise<ListTicketsResponse>;
  UpdateTicket(request: UpdateTicketRequest): Promise<UpdateTicketResponse>;
  DeleteTicket(request: DeleteTicketRequest): Promise<wellKnownEmpty>;
  // AssignTicket sets (or clears, with assignee_id=0) the ticket's assignee.
  AssignTicket(request: AssignTicketRequest): Promise<AssignTicketResponse>;
  // UpdateTicketStatus transitions the ticket's lifecycle status.
  UpdateTicketStatus(request: UpdateTicketStatusRequest): Promise<UpdateTicketStatusResponse>;
}

export function createTicketServiceClient(
  handler: RequestHandler
): TicketService {
  return {
    CreateTicket(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tickets`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketService",
        method: "CreateTicket",
      }) as Promise<CreateTicketResponse>;
    },
    GetTicket(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tickets/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketService",
        method: "GetTicket",
      }) as Promise<GetTicketResponse>;
    },
    ListTickets(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tickets`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.priority) {
        queryParams.push(`priority=${encodeURIComponent(request.priority.toString())}`)
      }
      if (request.assigneeId) {
        queryParams.push(`assigneeId=${encodeURIComponent(request.assigneeId.toString())}`)
      }
      if (request.query) {
        queryParams.push(`query=${encodeURIComponent(request.query.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketService",
        method: "ListTickets",
      }) as Promise<ListTicketsResponse>;
    },
    UpdateTicket(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tickets/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "TicketService",
        method: "UpdateTicket",
      }) as Promise<UpdateTicketResponse>;
    },
    DeleteTicket(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tickets/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "TicketService",
        method: "DeleteTicket",
      }) as Promise<wellKnownEmpty>;
    },
    AssignTicket(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tickets/${request.id}/assign`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketService",
        method: "AssignTicket",
      }) as Promise<AssignTicketResponse>;
    },
    UpdateTicketStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tickets/${request.id}/status`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketService",
        method: "UpdateTicketStatus",
      }) as Promise<UpdateTicketStatusResponse>;
    },
  };
}
export type ListTagsRequest = {
  kind?: string;
};

export type ListTagsResponse = {
  tags: TicketTag[] | undefined;
};

export type CreateTagRequest = {
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  kind: string | undefined;
  color: string | undefined;
  description: string | undefined;
};

export type CreateTagResponse = {
  tag: TicketTag | undefined;
};

export type UpdateTagRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  name?: string;
  color?: string;
  description?: string;
};

export type UpdateTagResponse = {
  tag: TicketTag | undefined;
};

export type DeleteTagRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type SetTicketTagsRequest = {
  //
  // Behaviors: REQUIRED
  ticketId: string | undefined;
  tagIds: string[] | undefined;
};

export type SetTicketTagsResponse = {
  ticket: Ticket | undefined;
};

export interface TicketTagService {
  ListTags(request: ListTagsRequest): Promise<ListTagsResponse>;
  CreateTag(request: CreateTagRequest): Promise<CreateTagResponse>;
  UpdateTag(request: UpdateTagRequest): Promise<UpdateTagResponse>;
  DeleteTag(request: DeleteTagRequest): Promise<wellKnownEmpty>;
  // SetTicketTags replaces a ticket's tags with the given tag ids (manual tagging).
  SetTicketTags(request: SetTicketTagsRequest): Promise<SetTicketTagsResponse>;
}

export function createTicketTagServiceClient(
  handler: RequestHandler
): TicketTagService {
  return {
    ListTags(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tags`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.kind) {
        queryParams.push(`kind=${encodeURIComponent(request.kind.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketTagService",
        method: "ListTags",
      }) as Promise<ListTagsResponse>;
    },
    CreateTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tags`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketTagService",
        method: "CreateTag",
      }) as Promise<CreateTagResponse>;
    },
    UpdateTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tags/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "TicketTagService",
        method: "UpdateTag",
      }) as Promise<UpdateTagResponse>;
    },
    DeleteTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tags/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "TicketTagService",
        method: "DeleteTag",
      }) as Promise<wellKnownEmpty>;
    },
    SetTicketTags(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.ticketId) {
        throw new Error("missing required field request.ticket_id");
      }
      const path = `v1/tickets/${request.ticketId}/tags`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TicketTagService",
        method: "SetTicketTags",
      }) as Promise<SetTicketTagsResponse>;
    },
  };
}
export type TicketErrorReason =
  // 400 - Bad Request
  | "BAD_REQUEST"
  | "INVALID_STATUS"
  | "INVALID_ASSIGNEE"
  // 401 / 403
  | "UNAUTHORIZED"
  | "FORBIDDEN"
  | "ACCESS_DENIED"
  // 404 - Not Found
  | "NOT_FOUND"
  | "TICKET_NOT_FOUND"
  | "COMMENT_NOT_FOUND"
  // 409 - Conflict
  | "TICKET_ALREADY_EXISTS"
  // 500 - Internal Server Error
  | "INTERNAL_SERVER_ERROR"
  | "DATABASE_ERROR"
  // 503 - Service Unavailable
  | "SERVICE_UNAVAILABLE"
  | "ADMIN_UNAVAILABLE";
// AssignableUser is a thin projection of an admin-service user, used to
// populate the assignee dropdown in the ticket UI.
export type AssignableUser = {
  id: number | undefined;
  username: string | undefined;
  name: string | undefined;
  email: string | undefined;
};

export type ListAssignableUsersRequest = {
};

export type ListAssignableUsersResponse = {
  users: AssignableUser[] | undefined;
};

export interface TicketUserService {
  // ListAssignableUsers returns the users (in the caller's tenant) a ticket
  // can be assigned to. Proxied to admin-service.
  ListAssignableUsers(request: ListAssignableUsersRequest): Promise<ListAssignableUsersResponse>;
}

export function createTicketUserServiceClient(
  handler: RequestHandler
): TicketUserService {
  return {
    ListAssignableUsers(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/assignable-users`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TicketUserService",
        method: "ListAssignableUsers",
      }) as Promise<ListAssignableUsersResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
